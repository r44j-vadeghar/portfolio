---
import { Block } from "astro-portabletext/components";
import type { Props } from "astro-portabletext/types";

const props = Astro.props;
---

<Block id={props.node._key} {...props}>
  <slot />
</Block>
